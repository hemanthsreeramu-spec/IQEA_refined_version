YOUR TASK:
Generate a COMPLETE, runnable, production-ready Python pytest test script using Playwright (sync by default).

INPUTS (provide when invoking the LLM):
- test_file_language = Python-Playwright
- page_files_content = {page_files_content}         # string containing one or more page file sources
- test_files_content = {test_files_content}         # structured test cases / steps / expected results
- recorded_actions (optional) = {recorded_actions}  # optional recorded interaction log

MUST-FOLLOW EXECUTION ORDER & PREPROCESSING:
1. Parse test_files_content → extract all test cases, sequential steps, expected validations, input values, and determine the primary assertion for each test.
2. Parse recorded_actions → detect base URL (first recorded URL only), user flows, element interactions, input values, and window/tab opens.
3. Parse page_files_content thoroughly BEFORE producing code:
   - Extract exact module/file name (file_key) for each page file.
   - Extract exact class name (class_name) as declared.
   - Extract all method names and exact parameter signatures.
   - Extract constructor signature(s) and all locator definitions (locator keys, field names, or locator strings).
   - DO NOT rename, append, change casing, or otherwise alter any POM identifiers — use them exactly as provided.
4. Only after steps 1–3, generate the final executable pytest script.
PLAYWRIGHT LOCATOR HANDLING (CRITICAL)
For Python-Playwright and Java-Playwright:
- All locators must be instantiated as Locator objects inside __init__.
- Do NOT create methods returning locators.
- Do NOT wrap locators in functions.
- Store locators ONLY like:
  self.<locator_name> = page.locator("<selector>")
- All interaction methods (click, fill, select, etc.) must directly call:
  self.<locator_name>.click()
  self.<locator_name>.fill(value)
  self.<locator_name>.wait_for(state="visible")
  etc.
- wait_for_element MUST assume locator_key maps to a Locator object:
  def wait_for_element(self, locator_key: str):
      locator = getattr(self, locator_key)
      locator.wait_for(state="visible")
      return locator
Without this rule, Playwright tests WILL FAIL because locators cannot be functions.

POM IMPORTS (EXACT & MINIMAL):
- For each POM referenced by tests import exactly:
  from output.page_file_generator.<file_key> import <class_name>
- Import only the POMs actually used in the generated tests.
- If a POM class referenced in test_files_content is absent from page_files_content, DO NOT invent names — create a local helper (see HELPER RULES).
XPATH HANDLING — UPDATED RULE
- Use EXACT locator strings extracted from the POM page files. Do NOT invent your own XPath.
- If the extracted locator is empty or incorrect:
    1. sleep(0.5–1 sec)
    2. retry the same locator
    3. fallback to locator from recorded_actions
    4. if still failing, auto-generate a stable locator:
        - id / name / data-* attributes
        - placeholder
        - label or visible text
- Every fallback MUST be wrapped inside:
    with allure.step("healing: <description>"):
TEST STRUCTURE (pytest-playwright & fixtures):
- Implement tests as pytest functions or classes following pytest conventions.
- Provide fixtures for browser/context/page setup (scope=function or session as appropriate). The fixture must:
  - Use sync_playwright() to create Playwright, launch **WebKit** browser (headless default configurable),
  - Create BrowserContext and Page,
  - Instantiate required POM classes using exactly the constructors parsed from page_files_content,
  - yield the Page (and POM instances if convenient), and close context/browser/playwright after yield.
- Avoid global sleeps in fixtures.

IMPORTS (DYNAMIC, MINIMAL & REQUIRED):
- Include only imports actually used. Typical allowed imports (add only when used):
  import pytest
  import time
  import json
  import os
  import allure
  from allure_commons.types import AttachmentType
  from playwright.sync_api import sync_playwright, Page, Locator
- POM imports as above.
- No unused imports.

ALLURE & REPORTING (MANDATORY):
- Wrap meaningful test steps in `with allure.step("..."):` blocks.
- Attach screenshots after important steps and after any healing attempt:
  allure.attach(page.screenshot(), name="step-name", attachment_type=allure.attachment_type.PNG)
- On assertion failure attach screenshot and page HTML (page.content()) to Allure. If Allure is not available at runtime, write a JSON + HTML snapshot to `/reports` and log its path.
- Ensure the script will populate `allure-results` at runtime via the above attachment calls. Do not include CLI instructions.

METHOD USAGE & HELPER RULES:
- For each test step, attempt to CALL the exact POM method (exact name and signature) parsed from page_files_content.
- If the exact POM method exists but is clearly inappropriate (incorrect signature/behavior), then:
  1) Attempt to call it once and log the failure in `allure.step` with screenshot;
  2) If it fails, create a local helper `helper_<action>_<file_key>` in the test file to perform the step.
- If the exact POM method is MISSING, immediately create `helper_<action>_<file_key>`.
- Helper naming convention: `helper_<action>_<file_key>` (snake_case).
- Helper implementation rules:
  - Use `page.locator("xpath=...")` or other locators defined in the POM.
  - Prefer POM locators; if none exist, use reliable selectors (id/name/data-*), then visible text.
  - Wrap helper execution in `with allure.step("..."):` and attach before/after screenshots.
  - Insert helpers inside the same test module — do not modify page files.

WINDOW / POPUP HANDLING (PLAYWRIGHT):
- NEVER switch to a new window immediately after page.goto().
- Only switch AFTER a user action that is known to open a new window (click, submit, link navigation).
- If popup/tab is expected:
    * Always use Playwright’s event-based popup listener:
        with page.expect_popup() as popup_info:
            <trigger-click>
        new_page = popup_info.value
        new_page.bring_to_front()
        time.sleep(0.5)

- If no popup is detected:
    pages = page.context.pages
    if len(pages) > 1:
        new_page = pages[-1]

- Recreate POM instances for new_page when required.
TIMING RULE — UPDATED
- Add time.sleep(0.5–1.0) ONLY:
    * after click that triggers a popup
    * after navigation-heavy actions
    * after a locator interaction fails (as retry)
- Do NOT add sleeps globally or before every step.
WAITING, RETRY & SELF-HEALING (MANDATORY):
Interaction algorithm per operation:
1. Try direct interaction (POM method or `page.locator(...).click()/fill()`).
2. If it fails, retry once after a short sleep (`time.sleep(0.5–1.0)`).
3. If still failing, fallback to `locator.wait_for(timeout=...)` or `page.wait_for_load_state()`.

If locator issues persist:
- Attempt alternate locator keys defined in the same POM (if any).
- Attempt up to 2 generated fallback selectors (id/name/data-* or visible text).
- Log each healing attempt as `with allure.step("healing: ..."):` and attach before/after screenshots.
SELF-HEAL RETRY
If click(), fill(), or any action fails:
    1. time.sleep(0.5–1 sec)
    2. locator.wait_for(state="visible", timeout=5000)
    3. retry the action
If still failing:
    - attempt fallback XPath or other available locators from POM
    - attempt stable locator generation
    - wrap every fix in allure.step("healing: ...")

POPUP PREVENTION RULE
- Do NOT call helper_switch_to_new_window() right after page.goto()
- Only call switch AFTER an action marked in test_files_content or recorded_actions as:
    * “opens new page”
    * “opens new tab”
    * “redirects”
    * “launches popup”
EXECUTION ORDER RULE
1. Perform interaction (click, fill, submit)
2. Wait or sleep ONLY if needed
3. Handle popup/tab if triggered
4. Reinstantiate POMs for the new page
5. Continue remaining steps
POST-GENERATION SELF-HEAL (MANDATORY):
After generating the test script, perform a static validation pass and auto-fix issues before returning code. Validation & fixes MUST include:
- Ensuring all imports used are present and minimal.
- Ensuring all referenced POM classes and constructor signatures exist in page_files_content; if a referenced POM is missing or constructor signature differs, create a local helper and adapt usage accordingly.
- Ensuring each method call maps to an existing method (exact name & signature). If not, create `helper_<action>_<file_key>` helpers as described above.
- Consolidating or removing duplicate assertions so each test contains EXACTLY ONE primary assertion.
- Ensuring no undefined variables remain.
- Ensuring popup handling uses Playwright popup constructs.
- Ensuring `allure-results` will be populated during execution (insert missing attachments where appropriate).
- If any automatic fix is applied, the generated script MUST include `allure.step(...)` calls documenting what was changed (so runtime logs show what was healed).
Return only the corrected, executable-ready script (raw code).

ASSERTIONS:
- Implement EXACTLY ONE primary assertion per test using `assert`.
- The primary assertion must reflect the Expected Result parsed from test_files_content.
- Additional non-primary checks are allowed only if they do not conflict with the single-primary-assertion rule.

FINAL VALIDATIONS (BEFORE RETURNING):
- All referenced POM classes/methods must exist or corresponding helpers must be implemented and logged via Allure steps.
- All imports used are present and stack-appropriate.
- No undefined variables remain.
- No unused imports remain.
- Popup flows are wrapped with Playwright popup constructs where applicable.
- `allure-results` will be populated with attachments during script execution.
- Each test contains exactly one primary assertion.

OUTPUT RULE (ABSOLUTE):
- RETURN ONLY RAW CODE. Do NOT wrap output in triple backticks like ```language,```java,```python,```.
- Do NOT include explanations, comments, or markdown.
- Do NOT wrap in triple backticks.

NOW GENERATE the final Python Playwright pytest script using the supplied inputs.
