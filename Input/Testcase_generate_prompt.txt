You are an expert QA Test Case Generator.

Your task is to generate a comprehensive, logically ordered, end-to-end set of functional test cases using the following inputs:

Inputs:
- Navigation Flow: {navigation}
- UI Layout from Screen Images: {image_data_processed}
{action_data_processed}

Requirements:
{requirements}

Instructions:
1. Create complete user journeys as test cases, **starting from login or the initial screen and ending at the final screen (e.g., confirmation, success, or summary)**.
2. **At least one test case must represent a full end-to-end flow across all screens.**
3. If recorded user actions (`action_data_processed`) are available, use them to guide step-by-step flows. Otherwise, infer the realistic order from `navigation` and `image_data_processed`.
4. Do not skip any necessary steps like login, intermediate forms, or navigation transitions.
5. Ensure that test cases collectively cover all key UI elements like buttons, fields, dropdowns, tables, popups, etc.
6. Include a diverse mix of test types: positive, negative, edge cases, retries, invalid inputs, and boundary scenarios.
7. Use orthogonal pairwise input coverage to minimize redundancy but ensure coverage.
8. Cover both frontend behavior (validations, messages) and backend/system outcomes (data saved, redirection, etc.)

Output Format:
- Return the test cases strictly as Markdown-style tables using these columns:
  `Test Case Name | Step Number | Test Step Description | Test Step Expected Result | Status | Type`
- Status should be `"New"` and Type should be `"Manual"`
- Include **15–20 test cases total**, each with **2–6 steps**
- Ensure **at least one test case spans the full end-to-end flow**
- Do not include any explanations, markdown headings, or summaries—**only return the tables**

Begin now:
Generate test cases based on the above inputs.
