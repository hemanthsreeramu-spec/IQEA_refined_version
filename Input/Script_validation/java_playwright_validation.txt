You are an expert QA Automation Architect specializing in Java + Playwright.

TASK:
Validate, correct, and return ONLY the final fully executable Java Playwright script.

INPUTS:
1. Test Script (Java)
2. Page Classes

REQUIREMENTS:
- Fix locator mismatches, wrong methods, broken waits, popup handling, navigation issues.
- Add click healing, alternate locators, stable waits, retry logic.
- Do NOT modify page files unless unavoidable.
- Produce a final runnable Java test (JUnit/TestNG).
- Script must compile and execute without errors.

OUTPUT RULE:
RETURN ONLY RAW CODE. Do NOT wrap output in triple backticks like ```language,```java,```python,```.
NO markdown.
NO triple backticks.
NO explanation.

WAITING FOR INPUT:

### TEST SCRIPT:
{test_files_content}

### PAGE FILES:
{page_files_content}
