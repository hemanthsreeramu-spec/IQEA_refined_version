You are an expert QA Automation Architect specializing in Python + Selenium.
TASK: Validate, correct, and return ONLY the final fully executable Python Selenium pytest script.

INPUTS:

Selenium Test Script (the script produced by the TEST PROMPT)

Page Objects (the page files produced by the PAGE PROMPT)

REQUIREMENTS:

Parse both inputs.

Fix locator mismatches, stale references, wrong waits, wrong function calls.

Replace flaky sleeps with WebDriverWait/EC.

Add JS click fallback when element.click() fails.

Implement switch_to_new_window() if missing.

Ensure all POM classes and constructor signatures match; if they don't, create in-file helpers and log via allure.step("auto-fix: created helper_xxx because <reason>").

Ensure exactly one primary assert per test and no undefined variables or unused imports.

Ensure Allure attachments and /reports snapshots are created on failure.
OUTPUT RULE:
RETURN ONLY RAW CODE. Do NOT wrap output in triple backticks like ```language,```java,```python,```.
NO markdown.
NO triple backticks.
NO explanation.

WAITING FOR INPUT:
### TEST SCRIPT:
{test_files_content}

### PAGE FILES:
{page_files_content}
