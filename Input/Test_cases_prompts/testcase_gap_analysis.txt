You are a QA analyst performing a gap analysis between existing test cases and a new application recording.

## Existing Test Cases (from Test Management Tool):
{existing_tcs}

## New Application Data:

### Recorded Actions:
{action_data}

### UI Screenshot Context:
{image_data}

### Requirements / User Stories:
{requirements}

## Your Task:
Compare the existing test cases against the new application data and identify:

1. **NEW** — Scenarios that are NOT covered by any existing test case (new pages, new fields, new flows, new validations discovered in the recording).
2. **UPDATE** — Existing test cases that need to be revised because the recording shows changes (e.g., a field was renamed, a new step was added, a URL changed, a validation message changed).
3. **SKIP** — Existing test cases that are already fully covered and need no changes.

## Rules:
- Base your analysis on the actions, UI fields, and requirements provided — not assumptions.
- For UPDATE entries, be specific about what changed so the tester knows exactly what to revise.
- Keep NEW scenario descriptions concise (1 sentence each) — they will be used as generation prompts.
- Do not invent scenarios that cannot be derived from the provided data.

## Output Format:
Return ONLY a valid JSON object. No markdown fences, no explanations.

{{
  "new": [
    "Login with locked-out user account should display an error message",
    "Add multiple items to cart and verify the cart badge count updates correctly"
  ],
  "update": [
    {{
      "id": "TC-12",
      "title": "Valid login flow",
      "reason": "New 2FA verification step detected on login page after password entry"
    }},
    {{
      "id": "TC-18",
      "title": "Checkout flow",
      "reason": "Billing address field added to checkout form — existing steps are incomplete"
    }}
  ],
  "skip": ["TC-05", "TC-09", "TC-14"]
}}
