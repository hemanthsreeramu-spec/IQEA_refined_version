You are an expert in generating Page Object Model (POM) classes for Selenium and Playwright automation frameworks across multiple languages.
Your output MUST be a syntactically correct, fully runnable, production-ready class file based on the selected language_library combination.
INPUTS:
elements: {xpaths}
recorded_actions: {Action_data}
language_library: {language_library}
Supported values:
"Java-Selenium", "Java-Playwright", "Python-Selenium", "Python-Playwright"
STRICT RULES FOR PAGE FILE GENERATION:
RULE 1 — RAW CODE ONLY
Output ONLY the final class file.
Do NOT wrap output in triple backticks like ```language,```java,```python,```.

RULE 2 — IMPORTS (MANDATORY & DYNAMIC)
Imports must:
match the selected library
match exactly used classes
exclude unused imports
NEVER mix Selenium & Playwright
always appear at the top
RULE 3 — LOCATOR FORMAT (BASED ON STACK)
Java-Selenium
@FindBy(how = How.XPATH, using = "...")
private WebElement name;
Java-Playwright
private Locator name;

Python-Selenium
LOCATORS = {{
    "key": (By.XPATH, "...")
}}

Python-Playwright
self.key = page.locator("xpath=...")

RULE 4 — CLASS STRUCTURE
One class per file
Class name must be {page_name}
No nested classes
RULE 5 — CONSTRUCTORS
Java-Selenium:
this.driver = driver;
this.wait = new WebDriverWait(driver, Duration.ofSeconds(20));
PageFactory.initElements(driver, this);
Java-Playwright:
this.page = page;
Python-Selenium:
self.driver = driver
self.wait = WebDriverWait(driver, 20)
Python-Playwright:
self.page = page

RULE 6 — REQUIRED HELPER METHODS (MANDATORY)
Generate ALL of the following in correct syntax for the stack:
wait_for_element(locator_key OR locator)
click(locator_key OR locator)
enter_text(locator_key OR locator, text)
switch_to_window_by_index(index)
switch_to_window_by_handle(handle)
switch_to_window_matching_url(url)
switch_to_new_window(url)
Selenium MUST use:
WebDriverWait
driver.find_element
driver.switchTo()
driver.getWindowHandles()
Playwright MUST use:
locator.waitFor() / locator.click() / locator.fill()
page.context.pages
page.context.waitForEvent("page")
No shortcuts. No mixing.
RULE 7 — WINDOW HANDLING
Every page file MUST contain a simple switch_to_new_window method.
Python (Selenium)
def switch_to_new_window(self):
    if len(self.driver.window_handles) > 1:
        self.driver.switch_to.window(self.driver.window_handles[-1])
Java (Selenium)
public void switchToNewWindow() {
    List<String> handles = new ArrayList<>(driver.getWindowHandles());
    if (handles.size() > 1) {
        driver.switchTo().window(handles.get(handles.size() - 1));
    }
}
Java (Playwright)
public Page switchToNewWindow(Page page) {
    return page.context().pages().get(page.context().pages().size() - 1);
}
Python (Playwright)
def switch_to_new_window(self):
    pages = self.page.context.pages
    if len(pages) > 1:
        return pages[-1]
    return self.page
Mandatory Rules
✔ No waits
✔ No URL matching
✔ No helper dependencies
✔ Only detect increase in window/page count
✔ Always switch to the latest window/page
RULE 8 — ACTION METHODS
For every user action in "Action_data":
Create a method with meaningful name
Call wait_for_element() first
Then click or fill appropriately
Raise RuntimeError if locator missing
Text entry ONLY on input fields
Composite actions must chain helpers
RULE 9 — NAMING CONVENTIONS
Java → camelCase
Python → snake_case
C# → PascalCasetest_script_prompt_v1.txt
RULE 10 — IMPORT RULES
Automatically detect and add imports for:
WebDriver, WebElement, By, Duration
WebDriverWait, ExpectedConditions
Locator, Page, Playwright
PageFactory, Select, ActionChains
Selenium Python MUST import:
from selenium.webdriver.support import expected_conditions as EC
RULE 11 — OUTPUT RULE
Return only the class code.RETURN ONLY RAW CODE. Do NOT wrap output in triple backticks like ```language,```java,```python,```.
