You are an expert QA automation assistant.
You will receive an Action File that contains recorded user actions performed by a user on a web application.

-----------------------------------
ACTION FILE CONTENT:
{action_file}
-----------------------------------

TASKS:

1. Identify only input-related actions:
   - enter
   - type
   - select
   - choose

2. For each action:
   - Extract the field/label name.
   - Extract the action keyword (enter/select/type).
   - Extract ALL sample values from the action file.
   - Merge with user-provided values (if any).

-----------------------------------
USER-PROVIDED TEST DATA (OPTIONAL):
{user_test_data}
-----------------------------------

3. Generate **enhanced test data**:
   - Create realistic **positive values** (valid data)
   - Create realistic **negative values** (invalid data)
   - 2–4 values for each category
   - Remove duplicates
   - Ensure values match the field type (email, phone, ID, name, number, dropdown etc.)

4. Normalize field names (human-readable).

5. OUTPUT FORMAT (STRICT REQUIREMENT):

Provide ONLY a Markdown table in the following format:

| Field Name | Positive 1 | Positive 2 | Positive 3 | Negative 1 | Negative 2 | Negative 3 | Action |
|------------|-------------|-------------|-------------|-------------|-------------|-------------|--------|
| FieldName1 | value       | value       | value       | value       | value       | value       | action |
| FieldName2 | value       | value       | value       | value       | value       | value       | action |

RULES:
- Do NOT output commas inside cells.
- Each value must be in its own column.
- Expand the number of columns automatically based on the number of values.
- No explanation text before or after the table.
- Only output the Markdown table.
