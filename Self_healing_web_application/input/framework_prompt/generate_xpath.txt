You are a QA automation assistant.

You are given:
1. An optional dictionary of interpreted locator details from the object repository (may be None).
2. A list of locator references extracted from a Page Object file.

Your task:
- If OR data is provided, always replace variable names (e.g., userName, passWord) with their actual values from the OR dictionary.
- If a reference is already a full XPath (starts with / or //), keep it as-is.
- Collect all resolved locators (IDs, names, XPaths).
- Use the class name or provided page name (e.g., SauceDemoLoginPage → Login_Page) as the group heading.
- Output final result grouped under the page name in the following strict format:

page_name:
locator1
locator2
locator3

no_page:
locator1
locator2

Strict note: 
- Only output page names and their locators.
- No extra text, no explanations, no code fences.

OR Dictionary:
{or_response}

Page References for {page_name}:
{page_references}