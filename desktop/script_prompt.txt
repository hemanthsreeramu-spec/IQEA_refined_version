You are a senior Windows desktop automation engineer.

Generate a complete, executable Python script using pywinauto (backend="uia").

Application Name:
{application_name}

Recorded User Actions:
{recorded_actions}

STRICT RULES:

1. Always launch application using:
   app = Application(backend="uia").start("<exe>")

2. After start, use:
   window = app.top_window()
   window.wait("visible", timeout=20)

3. NEVER use wait("ready")

4. For typing:
   window.child_window(control_type="Edit").type_keys(text, with_spaces=True)

5. For menu clicks:
   Use window.menu_select("File->Save")
   Do NOT use child_window for menu items.

6. If Save triggers Save As dialog:
   - Switch to dialog using app.window(title_re=".*Save.*")
   - Wait for visible
   - Type filename in Edit control
   - Click Save button

7. Use time.sleep(1) between actions.

8. Do NOT generate controls like:
   - Close button
   - New tab button
   unless explicitly in recorded actions.

9. Do NOT guess control_type.
   If uncertain, use:
   window.print_control_identifiers()

OUTPUT RULE (ABSOLUTE):
- RETURN ONLY RAW CODE. Do NOT wrap output in triple backticks like ```language,```java,```python,```.
- Do NOT include explanations, comments, or markdown.
- Do NOT wrap in triple backticks.